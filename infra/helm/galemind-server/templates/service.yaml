apiVersion: v1
kind: Service
metadata:
  name: {{ include "galemind-server.fullname" . }}
  labels:
    {{- include "galemind-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "galemind-server.selectorLabels" . | nindent 4 }}